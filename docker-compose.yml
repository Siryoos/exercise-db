services:
  crawler:
    build: .
    container_name: exercise_crawler
    volumes:
      - ./dist:/app/dist
    environment:
      - PYTHONPATH=/app

volumes:
  postgres_data:
  redis_data:
  es_data: 